
// react 0.14 - stateless function component
// https://goo.gl/E3kDlv
export default props => {
      return <div>NotFound.jsx > 找不到頁面 404</div>;
}

/*
var React = require('react');
export default React.createClass({
	render: function () {
    },
});

var Aquarium = (props) => {
  var fish = getFish(props.species);
  return <Tank>{fish}</Tank>;
};

*/
